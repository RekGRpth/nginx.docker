name: Docker
on:
  push:
    branches:
      - latest
  repository_dispatch:
    types:
      - latest
  workflow_dispatch:
env:
  GITHUB_TOKEN: ${{ github.token }}
concurrency:
  cancel-in-progress: true
  group: ${{ github.repository }}/${{ github.job }}
jobs:
  latest:
    name: ${{ github.job }} ${{ github.event.client_payload.repository }} ${{ github.event_name }} ${{ github.event.branch }}
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/git-clone-shell-action@v1
      - uses: rekgrpth/docker-login-build-push-shell-action@v1
      - uses: rekgrpth/github-publish-action-shell-action@v1
    timeout-minutes: 20
  test-latest:
    env:
      DOCKER_BUILD: test
      DOCKER_FROM: nginx.docker:latest
      INPUTS_TAG: test-latest
    needs: latest
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/git-clone-shell-action@v1
      - uses: rekgrpth/docker-login-build-push-shell-action@v1
    timeout-minutes: 20
  edge:
    env:
      DOCKER_FROM: lib.docker:edge
      INPUTS_TAG: edge
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/git-clone-shell-action@v1
      - uses: rekgrpth/docker-login-build-push-shell-action@v1
      - uses: rekgrpth/github-publish-action-shell-action@v1
    timeout-minutes: 20
  test-edge:
    env:
      DOCKER_BUILD: test
      DOCKER_FROM: nginx.docker:edge
      INPUTS_TAG: test-edge
    needs: edge
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/git-clone-shell-action@v1
      - uses: rekgrpth/docker-login-build-push-shell-action@v1
    timeout-minutes: 20
  debian:
    env:
      DOCKER_FROM: lib.docker:debian
      INPUTS_DOCKERFILE: Dockerfile.debian
      INPUTS_TAG: debian
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/git-clone-shell-action@v1
      - uses: rekgrpth/docker-login-build-push-shell-action@v1
      - uses: rekgrpth/github-publish-action-shell-action@v1
    timeout-minutes: 20
  test-debian:
    env:
      DOCKER_BUILD: test
      DOCKER_FROM: nginx.docker:debian
      INPUTS_DOCKERFILE: Dockerfile.debian
      INPUTS_TAG: test-debian
    needs: debian
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/git-clone-shell-action@v1
      - uses: rekgrpth/docker-login-build-push-shell-action@v1
    timeout-minutes: 20
  ubuntu:
    env:
      DOCKER_FROM: lib.docker:ubuntu
      INPUTS_DOCKERFILE: Dockerfile.debian
      INPUTS_TAG: ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/git-clone-shell-action@v1
      - uses: rekgrpth/docker-login-build-push-shell-action@v1
      - uses: rekgrpth/github-publish-action-shell-action@v1
    timeout-minutes: 20
  test-ubuntu:
    env:
      DOCKER_BUILD: test
      DOCKER_FROM: nginx.docker:ubuntu
      INPUTS_DOCKERFILE: Dockerfile.debian
      INPUTS_TAG: test-ubuntu
    needs: ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/git-clone-shell-action@v1
      - uses: rekgrpth/docker-login-build-push-shell-action@v1
    timeout-minutes: 20
  strip:
    needs: latest
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/strip-docker-image-shell-action@v1
        with:
          args: |
            -f /etc/group \
            -f /etc/nginx \
            -f /etc/passwd \
            -f /etc/ssl \
            -f /usr/lib/engines-1.1 \
            -f /usr/local/bin/nginx \
            -f /usr/local/lib \
            -f /usr/local/share \
            -f /usr/share/ca-certificates \
            -f /var/cache/nginx \
            -f /var/log/nginx \
            -f /var/run \
            -v
